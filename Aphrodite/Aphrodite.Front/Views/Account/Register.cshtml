@model Aphrodite.Front.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="container">
        <div class="row">
        <h2>@ViewBag.Title.</h2>
        @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.AntiForgeryToken()
            <h4>Maak een nieuw FaceDate account aan.</h4>
            <hr />
            @Html.ValidationSummary("", new { @class = "text-danger" })
            <div class="form-horizontal">
                <div class="form-group">
                   @Html.TextBoxFor(m => m.Firstname, new { @class = "form-control", placeholder="Gebruikersnaam" })
                </div>
                <div class="form-group">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "E-mailadres" })
                </div>
                <div class="form-group">
                    <div class="col-sm-12">
                    <label for="gender">Geslacht</label>
                        </div>
                    <label class="radio-inline">
                        @Html.RadioButtonFor(m => m.Gender, "0", new { @class = "form0control" }) Man
                    </label>
                    <label class="radio-inline">
                        @Html.RadioButtonFor(m => m.Gender, "1", new { @class = "form0control" }) Vrouw
                    </label>
                </div>
                <div class="form-group">
                    @Html.TextBoxFor(m => m.BirthDay, new { @class = "form-control", placeholder = "Geboortedatum DD-MM-YYYY" })
                </div>
                <div class="form-group">
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Wachtwoord" })
                </div>
                <div class="form-group">
                        @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = "Herhaal wachtwoord" })
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" class="btn btn-default" value="Register" />
                    </div>
                </div>
            </div>
        }
            </div>
</div>

        @section Scripts {
            @Scripts.Render("~/bundles/jqueryval")
        }
</body>
</html>